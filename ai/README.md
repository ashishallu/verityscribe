# VerityScribe AI services

This package is server-only. `speech`, `verification`, `rag`, `embeddings`, `ocr`, `medicine_scanner`, and `chat` services must be invoked by FastAPI services using environment-provided credentials. No mobile or web client may access model keys.
