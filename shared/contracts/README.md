# Canonical contracts

The canonical network contract is the FastAPI OpenAPI document at `/api/v1/openapi.json`. Client-specific models must be generated or mapped from that document; no service keys or database types belong in clients.
